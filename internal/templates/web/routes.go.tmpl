{{ if eq .Recipe.Layout.Style "minimal" -}}
package app
{{ else -}}
package server
{{ end }}

{{ if eq .Recipe.Server.Framework "nethttp" -}}
import (
	"net/http"

{{ if ne .Recipe.Layout.Style "minimal" -}}
{{ if .Recipe.Observability.Health -}}
	"{{ .ModulePath }}/internal/server/handler"
{{ end -}}
	"{{ .ModulePath }}/internal/server/middleware"
{{ end -}}
)

func (s *Server) routes() http.Handler {
	mux := http.NewServeMux()
{{ if .Recipe.Observability.Health }}
{{ if eq .Recipe.Layout.Style "minimal" -}}
	mux.HandleFunc("/health", Health)
{{ else -}}
	mux.HandleFunc("/health", handler.Health)
{{ end -}}
{{ end -}}
{{ if .Recipe.Observability.Readiness }}
	mux.HandleFunc("/ready", ReadyHandler(s.readiness))
{{ end }}
	var h http.Handler = mux
{{ if eq .Recipe.Layout.Style "minimal" -}}
	h = Logging(s.logger)(h)
	h = RequestID(h)
	h = Recover(s.logger)(h)
{{ else -}}
	h = middleware.Logging(s.logger)(h)
	h = middleware.RequestID(h)
	h = middleware.Recover(s.logger)(h)
{{ end -}}
	return h
}
{{ end -}}
{{ if eq .Recipe.Server.Framework "chi" -}}
import (
	"net/http"

	"github.com/go-chi/chi/v5"
{{ if ne .Recipe.Layout.Style "minimal" }}
{{ if .Recipe.Observability.Health -}}
	"{{ .ModulePath }}/internal/server/handler"
{{ end -}}
	"{{ .ModulePath }}/internal/server/middleware"
{{ end -}}
)

func (s *Server) routes() http.Handler {
	router := chi.NewRouter()
{{ if eq .Recipe.Layout.Style "minimal" -}}
	router.Use(Recover(s.logger))
	router.Use(RequestID)
	router.Use(Logging(s.logger))
{{ else -}}
	router.Use(middleware.Recover(s.logger))
	router.Use(middleware.RequestID)
	router.Use(middleware.Logging(s.logger))
{{ end -}}
{{ if .Recipe.Observability.Health }}
{{ if eq .Recipe.Layout.Style "minimal" -}}
	router.Get("/health", Health)
{{ else -}}
	router.Get("/health", handler.Health)
{{ end -}}
{{ end -}}
{{ if .Recipe.Observability.Readiness }}
	router.Get("/ready", ReadyHandler(s.readiness))
{{ end }}
	return router
}
{{ end -}}
{{ if eq .Recipe.Server.Framework "gin" -}}
import (
	"net/http"

	"github.com/gin-gonic/gin"
{{ if ne .Recipe.Layout.Style "minimal" }}
{{ if .Recipe.Observability.Health -}}
	"{{ .ModulePath }}/internal/server/handler"
{{ end -}}
	"{{ .ModulePath }}/internal/server/middleware"
{{ end -}}
)

func (s *Server) routes() http.Handler {
	gin.SetMode(gin.ReleaseMode)
	router := gin.New()
{{ if eq .Recipe.Layout.Style "minimal" -}}
	router.Use(Recover(s.logger), RequestID(), Logging(s.logger))
{{ else -}}
	router.Use(middleware.Recover(s.logger), middleware.RequestID(), middleware.Logging(s.logger))
{{ end -}}
{{ if .Recipe.Observability.Health }}
{{ if eq .Recipe.Layout.Style "minimal" -}}
	router.GET("/health", Health)
{{ else -}}
	router.GET("/health", handler.Health)
{{ end -}}
{{ end -}}
{{ if .Recipe.Observability.Readiness }}
	router.GET("/ready", ReadyHandler(s.readiness))
{{ end }}
	return router
}
{{ end -}}
{{ if eq .Recipe.Server.Framework "echo" -}}
{{ if ne .Recipe.Layout.Style "minimal" -}}
import (
{{ if .Recipe.Observability.Health -}}
	"{{ .ModulePath }}/internal/server/handler"
{{ end -}}
	"{{ .ModulePath }}/internal/server/middleware"
)
{{ end }}
func (s *Server) registerRoutes() {
{{ if eq .Recipe.Layout.Style "minimal" -}}
	s.echo.Use(Recover(s.logger))
	s.echo.Use(RequestID)
	s.echo.Use(Logging(s.logger))
{{ else -}}
	s.echo.Use(middleware.Recover(s.logger))
	s.echo.Use(middleware.RequestID)
	s.echo.Use(middleware.Logging(s.logger))
{{ end -}}
{{ if .Recipe.Observability.Health }}
{{ if eq .Recipe.Layout.Style "minimal" -}}
	s.echo.GET("/health", Health)
{{ else -}}
	s.echo.GET("/health", handler.Health)
{{ end -}}
{{ end -}}
{{ if .Recipe.Observability.Readiness }}
	s.echo.GET("/ready", ReadyHandler(s.readiness))
{{ end }}
}
{{ end -}}
{{ if eq .Recipe.Server.Framework "fiber" -}}
{{ if ne .Recipe.Layout.Style "minimal" -}}
import (
{{ if .Recipe.Observability.Health -}}
	"{{ .ModulePath }}/internal/server/handler"
{{ end -}}
	"{{ .ModulePath }}/internal/server/middleware"
)
{{ end }}
func (s *Server) registerRoutes() {
{{ if eq .Recipe.Layout.Style "minimal" -}}
	s.app.Use(Recover(s.logger))
	s.app.Use(RequestID)
	s.app.Use(Logging(s.logger))
{{ else -}}
	s.app.Use(middleware.Recover(s.logger))
	s.app.Use(middleware.RequestID)
	s.app.Use(middleware.Logging(s.logger))
{{ end -}}
{{ if .Recipe.Observability.Health }}
{{ if eq .Recipe.Layout.Style "minimal" -}}
	s.app.Get("/health", Health)
{{ else -}}
	s.app.Get("/health", handler.Health)
{{ end -}}
{{ end -}}
{{ if .Recipe.Observability.Readiness }}
	s.app.Get("/ready", ReadyHandler(s.readiness))
{{ end }}
}
{{ end -}}
