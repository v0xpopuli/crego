# orders-api

Module: `github.com/example/orders-api`

Generated by crego from a web project recipe.

## Stack

- Server: `fiber`
- Configuration: `env`
- Logging: `slog`
- Layout: `minimal`
- Task scheduler: `none`

- Docker: enabled

- Docker Compose: enabled





## Commands

```sh
make run
make test
make build
make docker-build
make docker-run
make compose-up
make compose-down
make compose-logs
```






## Docker

The generated `Dockerfile` builds `./cmd/orders-api` in a multi-stage Go image and runs the binary as a non-root user on port `8080`.


```sh
make docker-build
make docker-run
```



## Docker Compose

`docker-compose.yml` starts the app service.


```sh
make compose-up
make compose-logs
make compose-down
```




## Endpoints

- `GET /health`

- `GET /ready`


## Configuration

Configuration is loaded from environment variables.

```sh
APP_ENV=development SERVER_PORT=8080 LOGGING_FORMAT=text make run
```



